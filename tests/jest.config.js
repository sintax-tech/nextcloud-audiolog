// Jest config for the Audiolog frontend unit tests.
module.exports = {
    testEnvironment: 'jsdom',
    testMatch: ['<rootDir>/js/**/*.test.js'],
    rootDir: '..',
    transform: {},
    setupFilesAfterEach: [],
};
